#include <iostream>
#include <cstdio>
#include <string>
#include <cmath>
#include <queue>

using namespace std;

int main() {
	int n, i, sum;
	cin >> n;
	if (n == 0) cout << "YONSEI";
	else cout << "Leading the Way to the Future";
}