#include <iostream>
#include <cstdio>
#include <string>
#include <cmath>
#include <queue>
#include <stack>

using namespace std;

int main() {
	int n, i, x, y;
	cin >> n;
	for (i = 0; i < n; i++) {
		cin >> x >> y;
		cout<< x + y << endl;
	}
}